# 04 确定性层：重复片段检测（内容哈希）

Status: needs-triage

## 描述

新增 `lib/dupes.mjs`：内容哈希（规范化后行级/语句级）检测重复片段——文件间重复行块与重复率统计。零新增依赖；>2MB 文件跳过解析（同 01 策略）。

## 建议处置

- 只报达到阈值（如 ≥6 行连续重复）的片段，附文件:行号对；
- 输出：重复片段清单 + 按文件的重复率 Top N。

## 验收标准

- smoke 通过；重复 fixture 命中预期片段对。
