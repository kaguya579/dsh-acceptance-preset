# Roadmap: arch-quality-review 未完成清单与架构细化方向

- 更新：2026-08-17（第二轮增量真实验收通过，已推送双远端）
- 现状：第一刀 + 第二轮增量全部实现并真实验收通过（hutool 轮次2/轮次3），smoke 全绿，已推送 gitcode + github（main @ 85e8152）；剩余为候选未排期项。

## 〇、第二轮 grilling 决策（2026-08-16 夜，2026-08-17 已实施）

真实验证（hutool 轮次2）通过：产物直落交付物内 acceptance、6 文件文档组、依赖图 4626 边/0 环。新决策六条：

1. ✅ **产物目录自污染**：扫描器加「本轮产物目录排除」参数，交付物扫描跳过该子树（含压缩包内同路径）——ticket 10 已实现，smoke 场景 10 验证；
2. ✅ **unresolved 归口**：四分类——项目内引用→边；通配导入（`cn.foo.*`）→包级模块边；JDK/已知外部包→外部引用计数（不进 unresolved）；静态导入→忽略；unresolved 只留「应能定位却未定位」的真问题——ticket 11 已实现；
3. ✅ **架构细化下一刀**：①Ce/Ca 耦合 + 模块依赖矩阵 + 主序列距离；②孤儿模块 + 入口可达性；③模块级度量聚合表——ticket 12 已实现（lib/modules.mjs），smoke 场景 5b 验证；
4. ✅ **backlog 04 同批**：问题清单固定表格 + 修复状态四值 + 防假已修复护栏，固化进 agent 指引——ticket 13 已实现（系统提示更新）；
5. ✅ **参数调参**：阈值常量不动；重复片段上限 500→2000——ticket 13 已实现；
6. ✅ **流程**：先提交已验证成果（已执行），次日 to-spec → 开票（issues/10–13）→ 实现 1–5（全部完成，smoke 全绿）。

待办：重启 DSH 后真实验收会话验证修复状态口径与文档组效果。

## 一、第一刀收尾（最高优先）

1. **真实会话复验**：✅ 完成（hutool 轮次2，2026-08-16 22:08）——①产物直落 `D:\project\hutool\acceptance\hutool-轮次2`；②6 文件文档组齐全；③依赖图建出真实边（4626 边/342 模块/1530 模块边/0 环）；第二轮增量（排除/归口/耦合/可达性/模块表/修复状态口径）也已真实验收通过（2026-08-17）✓
2. 复验遗留：`hutool-轮次1` 旧产物（单报告时代）建议手动清理，留轮次2 作历史。

## 二、架构设计细化方向（候选，按性价比排序）

1. ✅ **耦合度量深化**：Ce/Ca、模块间依赖矩阵、主序列距离——已实现（lib/modules.mjs，第二轮增量）；
2. ✅ **图算法补全**：孤儿模块、入口可达性——已实现（computeReachability，第二轮增量）；
3. ✅ **模块级度量聚合表**：行数/函数数/复杂度/重复统计/边数——已实现（第二轮增量）；
4. **分层违规校验**：从架构文档提取预期分层（agent 语义层产出规则）→ 确定性层校验违规（deptrac/ArchUnit 思路）；需先定义「分层规则」的中间表示——未排期；
5. **依赖漂移检测**：轮次间依赖图 diff 内建进插件（决策 6 曾选 agent 侧对比，升为插件侧可复现）——未排期；
6. **报告可视化**（ticket 09）：HTML 依赖图/度量分布渲染，纯 JS/WASM 模板引擎——未排期；
7. **ATAM 骨架深化**：质量属性场景、敏感点/权衡点模板写进语义指引——未排期。

## 三、语义层增强

- ✅ backlog 04：整改清单结构化 + 修复状态规则（未修复/部分修复/已修复/无法判定 + 整改完成率）——已固化进 agent 指引（ticket 13），真实验收通过；
- 供应商档案模板（ticket 07，纯 agent 聚合）——未排期；
- 云端 Java 专项业务描述（ticket 08，纯文档）——未排期。

## 四、工程化与环境

- ✅ git：本轮全部改动已提交并推送（gitcode + github 双远端，main @ 85e8152）；
- 新语言支持（Python/Go 等，等实际交付物出现）+ 测试映射（源代码↔测试缺口）——均暂缓；
- `standard` preset 的 tool-web 仍 `fetch: false`（acceptance 已开）——需要时复制改；
- web_search 凭据由用户自管（DeepSeek key 已填、flash 档）；web_fetch 已挂载可用。

## 五、已拒绝的方向（ADR-0004/0005 记录）

- codegraph 类外部引擎、dependency-cruiser 混搭；优化项参与验收判定/独立评审报告；插件侧轮次 diff（第一刀版）。
