## Agent skills

### 问题追踪器

本仓库的 issue 和 spec 以 markdown 文件形式存放在 `.scratch/<feature-slug>/`（本地 markdown；GitCode/GitHub remote 不用于 issue 追踪）。详见 `docs/agents/issue-tracker.md`。

### 领域文档

单上下文：根目录一个 `CONTEXT.md`，ADR 存放在 `docs/adr/`。详见 `docs/agents/domain.md`。
