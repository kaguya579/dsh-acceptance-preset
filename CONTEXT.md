# Context: 交付验收（DSH agent 版）

甲方对供应商交付物做确定性事实提取与语义验收，产出验收结论与后续接手方案。
本工程是 DeepSeek Harness（DSH）的验收 preset 插件：确定性能力内建在插件中，语义判断由 agent 完成。

## Language

### 角色

**甲方**:
委托供应商开发软件、接收交付物并执行验收的一方，即本工具的使用者。
_Avoid_: 验收方（在需要区分角色时用"甲方"作为唯一称谓）

**供应商**:
承接开发并提交交付物的外部团队。
_Avoid_: 乙方, 外包方, vendor

**agent**:
在 DSH 会话中执行验收的模型智能体：调用验收工具取事实包，完成四类偏差、问题分级、业务画像、接手方案与验收结论。
_Avoid_: 工具内 LLM（历史阶段：语义判断曾在外部工具内由 LLM 串行完成，现已弃用）

### 验收对象

**交付物**:
供应商在验收节点提交的全部产出，包括代码（压缩包快照）、架构设计文档、产品文档、需求文档、测试报告等。
_Avoid_: 产出物, 交付内容, 交付包

**交付物目录**:
交付物所在的本地目录/压缩包路径。验收时由用户提供，与需求/合同目录是两个独立来源。
_Avoid_: 固定交付目录（本项目不预设任何固定数据根目录）

**基线**:
甲方的对照基准——需求文档、合同条款、结构化需求清单，补缺识别与基线偏差判断的依据。**可缺省**：小项目可能没有书面需求文档（如仅口头一条需求），无基线时跳过补缺识别与基线偏差检查，验收结论降级为验收建议。
_Avoid_: 标准, 需求基线（统一称"基线"）

**需求/合同目录**:
基线文件所在的本地目录/文件路径（JSON/md/docx）。验收时由用户提供，可选。
_Avoid_: 基线目录（与"基线"概念混用时统称"需求/合同目录"）

**云端**:
运行在服务器侧的系统部分（本领域通常为 Java + JavaScript/TypeScript）。
_Avoid_: 服务端, 后端

**车端**:
运行在车辆上的嵌入式系统部分（本领域通常为 C 语言）。
_Avoid_: 车载端, 嵌入式端, 终端

**UDS**:
ISO 14229 Unified Diagnostic Services，车辆诊断统一服务协议，诊断专项车端代码的协议基线。
_Avoid_: 14229（指代协议时统一称 UDS）

### 验收过程

**验收**:
甲方对交付物执行确定性事实提取与语义检查，并给出结论的过程。
_Avoid_: 检查, 审核, 复核（作为本流程统称时）

**确定性层**:
插件内建的确定性分析——安全解包、文档解析、tree-sitter 静态分析、架构事实提取、补缺识别、轮次快照与变更识别。绝不调用 LLM、绝不产出结论。
_Avoid_: 静态层（统称"确定性层"）

**agent 语义层**:
agent 基于事实包完成的语义判断——四类偏差识别、问题分级、业务画像、接手方案与验收结论。
_Avoid_: LLM 分析层

**事实包**:
确定性层一轮运行的机器可读产出（`确定性事实.json`，schema `acceptance-facts/2`）：解析摘要、静态事实、架构事实、基线缺项、变更识别、上轮问题清单；随本轮产物落盘并内联返回给 agent。历史 /1 轮次仍可互读。
_Avoid_: facts 输出, 分析结果

**架构事实**:
事实包中的架构级确定性事实——依赖图（跨文件引用与被引用、模块聚合、循环依赖、通配导入包级边、外部引用计数〔JDK/第三方/系统头〕、真未解析清单）、复杂度度量（函数/文件行数、圈复杂度、超阈值清单）、模块表（行数/函数数/复杂度、耦合 Ce·Ca、不稳定性、抽象度、主序列距离、重复统计）、孤儿模块与入口可达性、依赖清单（构建清单声明的第三方库与版本）、重复片段。仅供 agent 取证，绝不产出结论。
_Avoid_: 架构分析（与语义判断混淆时统称"架构事实"）

**验收轮次**:
一次完整验收运行及其产物。轮次间做变更识别与修复状态对比。按触发方式分两类：例行验收（月度/季度定期执行）与复验（打回整改后重新执行）。
_Avoid_: 验收批次, 轮

**例行验收**:
按固定周期（月度/季度）对当前交付状态执行的验收。
_Avoid_: 定期验收（"例行验收"为统一称谓）

**复验**:
验收打回后，供应商整改并重新交付，针对新一轮交付物执行的验收，重点追踪上轮问题的修复状态。
_Avoid_: 重新验收

**修复状态**:
复验时对上轮问题清单逐条评估的结论——未修复/部分修复/已修复/无法判定；「未检查」绝不表述为「已修复」。
_Avoid_: 修复情况

**整改完成率**:
复验轮次中已修复问题数占上轮问题清单总数的比例，作为整改时效的量化口径。

**变更识别**:
轮次间对比交付物，识别新增、修改、删除的内容。
_Avoid_: diff（作为过程统称时用"变更识别"）

**补缺**:
对照基线，识别交付物中缺失的项目。
_Avoid_: 缺项（指单条缺失项时可用"缺项"）

**偏差**:
交付物与基线之间、或交付物相互之间的不一致。四类：①文档间偏差——两份交付文档互相矛盾；②文档-代码偏差——文档描述与代码真实实现不符；③基线偏差——交付物与需求/合同不符；④缺项——基线要求但交付物没有。
_Avoid_: 差异, 出入

**问题分级**:
验收问题按严重程度分三级：阻断——关键交付物缺失或与合同核心条款不符，不解决不通过；严重——影响接手或主要功能的偏差/缺项；一般——次要偏差、文档小错误。
_Avoid_: 缺陷等级（统一用"问题分级"）

**优化项**:
有确定性证据支撑的改进建议条目，覆盖架构（依赖环、耦合、分层违规）、规模复杂度（超长函数/文件、圈复杂度超阈值）、工程卫生（重复代码、明显缺失测试）三类；分级高/中/低。咨询性内容：不影响验收结论、不进整改清单、不参与问题分级。风格类主观项（命名、风格、重构品味）不列入。
_Avoid_: 问题, 缺陷（优化项不是偏差、不是缺陷）

### 验收产物

**验收文档组**:
一轮验收 agent 产出的完整文档集（全部 markdown，写入本轮产物目录）——`00-验收总览.md`（判定与产物索引）、`01-交付物概况与业务画像.md`、`02-偏差明细与取证.md`、`03-问题清单.md`（供整改）、`04-架构与质量评审.md`、`05-接手方案.md`；每个文件承载对应章节的**细节**（证据、数据、逐条明细），总览只做结论与索引。
_Avoid_: 报告包, 文档集（统一称"验收文档组"）

**验收报告**:
验收文档组中的总览文件（`00-验收总览.md`）：判定（通过/打回）、分级问题摘要、各文档索引。各章节细节在文档组其余文件。由 agent 产出，写入本轮产物目录。
_Avoid_: 验收文档, 验收纪要

**验收结论**:
验收报告中的最终判定——通过、打回、无法判定，及其依据。仅用于提供了基线的轮次；无基线轮次的结论节为验收建议。无法判定：agent 未完成偏差分析时使用——未做语义检查时不给出通过结论。退出码语义由调用方约定。
_Avoid_: 验收结果

**验收建议**:
无基线轮次的结论节——取值建议通过/建议整改/无法判定；判定仅基于交付物内部一致性（文档间偏差、文档-代码偏差），并固定注明「未提供基线，未做基线对照」。
_Avoid_: 验收结论（无基线场景不称「验收结论」）

**业务画像**:
综合代码与文档提取的整体业务视图，供甲方理解系统与识别需求。包含：业务流程/能力地图、功能清单、架构概览、实际术语表。由 agent 基于事实包与原文取证产出。架构概览只描述「是什么」；「哪里不合理、怎么优化」见架构与质量评审。
_Avoid_: 业务梳理, 系统画像

**接手方案**:
基于技术栈、代码规模、模块复杂度与系统形态（云端/车端）生成的后续团队配置建议及交接风险点。由 agent 产出。
_Avoid_: 团队建议（"接手方案"为统一称谓）

**架构与质量评审**:
验收文档组中的文件（`04-架构与质量评审.md`）——架构梳理（依赖图/模块边界观察）、优化项清单（按高/中/低分组，每条=证据+判断+建议）、与接手方案的衔接。不影响验收结论，不进入整改清单。由 agent 产出。
_Avoid_: 架构评审报告（独立产物形态已弃，统一为文档组文件）

**质量属性场景**:
04 评审节中的结构化小节——把质量关切（性能、可维护性、可测试性等）写成可验证场景（场景描述 + 对应确定性证据），ATAM 骨架的组成单元。
_Avoid_: 场景描述（作为固定小节时统一称"质量属性场景"）

**敏感点/权衡点**:
04 评审节中的结构化小节——敏感点：架构决策中值得警惕的风险点；权衡点：两个质量属性之间的取舍。均须有确定性证据锚点，无证据不列。
_Avoid_: 风险点（与问题分级混淆时用"敏感点/权衡点"）

**供应商档案**:
同一供应商全部验收轮次的聚合视图——问题复发、整改时效、质量趋势。双层产出：确定性台账（`acceptance_supplier_profile` 工具聚合轮次记录与事实包）+ agent 基于台账与各轮问题清单写出的语义档案（`供应商档案.md`），供甲方管理多供应商时使用。

### 工具结构

**通用验收**:
与业务领域无关的基础验收能力——确定性事实提取与四类偏差、画像、结论的语义判断。无基线时以验收建议替代验收结论。
_Avoid_: 基础验收（"通用"与"专项"是配套的两个层级）

**领域专项**:
叠加在通用验收之上的领域化增强。当前唯一实现是诊断专项，未来可扩展。
_Avoid_: 子项目, 插件, profile

**诊断专项**:
针对车辆诊断领域（UDS / ISO 14229）的针对性解析与检查：UDS 协议一致性、诊断文档偏差、诊断需求覆盖、通信层一致性。
_Avoid_: 诊断子项目

**业务描述**:
以自然语言书写的领域知识文件（业务背景、检查重点、常见偏差），随插件工程分发（`profiles/<专项>/业务描述.md`）；agent 在相关交付物验收前先读它，并按检查重点逐项执行。专项扩展的主要方式。
_Avoid_: 规则, 规则模板（业务描述不要求结构化，是纯叙述性知识）
